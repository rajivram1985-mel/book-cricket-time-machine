{
  "name": "book-cricket-time-machine",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "serve:pwa": "vite build && vite preview --port 4300",
    "test": "vitest run",
    "voice:generate": "vite-node scripts/generate-voice-clips.ts"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}
